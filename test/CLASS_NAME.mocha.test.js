import * as CLASS_NAME from '../app/js/CLASS.js';
const assert = require('assert');

describe('CLASS_NAME', function () {
	/* * * * * * * * * * Start: CLASS_NAME Class Tests * * * * * * * * * */
	// Display warning if CLASS_NAME is overdue.
	describe('CLASS_NAME overdue', function () {
		it('[CLASS_NAME] DESCRIPTION', function () {
			assert.equal("ASSERT_EQUAL_TO_VALUE", CLASS_NAME.class_method(class_arguments));
		});
	});
	/* * * * * * * * * * End: CLASS_NAME Class Tests * * * * * * * * * */
});
